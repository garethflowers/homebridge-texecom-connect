/**
 * The name of the Platform, as used ins `config.json`.
 */
export const platformName: string = "TexecomConnect";

/**
 * The name of the Plugin.
 */
export const pluginName: string = "homebridge-texecom-connect";
