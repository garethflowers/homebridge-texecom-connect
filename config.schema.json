{
  "pluginAlias": "HomebridgeTexecomConnect",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
      "host": {
        "default": "localhost",
        "placeholder": "localhost",
        "format": "hostname",
        "required": true,
        "title": "IP Address / Hostname",
        "type": "string"
      },
      "port": {
        "default": 1001,
        "placeholder": 1001,
        "maximum": 65535,
        "required": true,
        "title": "IP Port",
        "type": "integer"
      }
    }
  }
}
